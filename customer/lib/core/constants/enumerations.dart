enum PackageType { parcel, groceries, general }

enum OrderType { send, recieve }

enum OrderState { pending, onTransit, delivered, cancelled }

enum VehicleType { bicycle, motorcycle, car, truck }

enum VerifyType { signup, resetPwd }

enum XtridelinkDocsType { legal, terms, privacy }

enum Rating { zero, one, two, three, four, five }

enum GeneralPackageTypes {
  clothing,
  electronics,
  jewelleryAccessories,
  documents,
  healthProducts,
  computerAccessories,
  phones,
  others,
  frozenFood,
  cookedFood,
  dryFood,
  miscellaneous
}
