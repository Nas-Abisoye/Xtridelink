import 'package:flutter_test/flutter_test.dart';

// Placeholder so `flutter test` has a green baseline. Replace with real
// unit/bloc tests (auth cubit, order tracking, token refresh) — see README.
void main() {
  test('sanity', () {
    expect(1 + 1, 2);
  });
}
